name: Test Syzkaller Integration

on:
  workflow_dispatch:

jobs:
  syzk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - name: Test pipeline
        run: |
          python3 ./fuzzer/syzkaller/list_to_unique.py <(./fuzzer/syzkaller/db_to_list.sh)
